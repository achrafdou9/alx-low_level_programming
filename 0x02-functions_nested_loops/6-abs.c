#include "main.h"

/**
 * _abs - computes value
 * of an integer.
 *
 * @i: nput numbers as an integer.
 *
 * return: absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}	
